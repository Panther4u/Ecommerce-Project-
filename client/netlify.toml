[build]
  base = "client"                  # Base directory for your project
  publish = "client/dist"          # Directory where Vite outputs the build
  functions = "client/netlify/functions" # Directory for Netlify functions
  command = "npm run build"        # Command to build your project
